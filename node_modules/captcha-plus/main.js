const fs = require("fs");
if (!fs.existsSync("./captchadata.json")) {
  console.log("Captcha Data File Created!");
  fs.writeFileSync("./captchadata.json","{}", { flag: 'wx' })
}

module.exports = {
  create : require('./module/create.js'),
  convert : require('./module/convert.js'),
  user : require('./module/user.js'),
  check : require('./module/check.js')
};