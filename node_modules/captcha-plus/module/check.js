const fs = require("fs");
const db = require("../utils/db.js");

module.exports = async function(id,code) {
  let check = false;
  let c = db.get(id);
  if(c == code){
    check = true;
  }
  return await check;
  
};
